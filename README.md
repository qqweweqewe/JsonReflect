# JsonReflect
A fairly simple yet effective **Java** library that adds serialization and de-serialization functionality using Java's **Reflection API**
## Introduction
**JsonReflect** is a java library that can be used to represent Java Objects as `JSON` data as well as to convert the `JSON` back to a Java Object